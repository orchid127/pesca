'use client';
import React from 'react'

const Button = () => {
    return (
        <div>
            <button onClick={() => console.log('Click')}>Start</button>
        </div>
    )
}

export default Button